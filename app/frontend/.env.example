# Backend API origin (no trailing slash). Set on Render as a build-time variable so requests go to the API host, not the static frontend.
VITE_API_URL=https://shepherd-backend-aj54.onrender.com

# Optional: older name, still supported if VITE_API_URL is unset
# VITE_API_BASE_URL=https://shepherd-backend-aj54.onrender.com
