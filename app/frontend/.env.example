# Local overrides (optional). Production builds use `.env.production` (committed) or CI `VITE_API_URL`.
# VITE_API_URL=https://127.0.0.1:8000

# Optional legacy name if VITE_API_URL is unset:
# VITE_API_BASE_URL=https://shepherd-backend-aj54.onrender.com
