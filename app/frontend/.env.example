# Backend API origin (no trailing slash). Required for production builds where the UI is on a different host than the API.
# On Render: set this in the frontend service as a build-time variable with the same name.
VITE_API_BASE_URL=https://shepherd-backend-aj54.onrender.com
