{
    "title": "user_roles",
    "type": "object",
    "properties": {
        "id": {
            "type": "integer",
            "description": "Primary key, auto-increment"
        },
        "user_id": {
            "type": "string",
            "description": "User ID from auth system"
        },
        "app_role": {
            "type": "string",
            "description": "Application role: admin, manager, or worker"
        },
        "display_name": {
            "type": "string",
            "description": "Display name for the user"
        },
        "created_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the role was assigned"
        },
        "updated_at": {
            "type": "string",
            "format": "date-time",
            "description": "When the role was last updated"
        }
    },
    "required": [
        "id",
        "user_id",
        "app_role"
    ]
}